volumes:
  elasticsearch-data:

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.3.3
    environment:
      discovery.type: single-node
      xpack.security.enabled: "true"
      xpack.security.http.ssl.enabled: "false"
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD:-changeme}
      ES_JAVA_OPTS: -Xms512m -Xmx512m
    ports:
      - "9200:9200"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data

  kibana:
    image: docker.elastic.co/kibana/kibana:9.3.3
    environment:
      ELASTICSEARCH_HOSTS: '["http://elasticsearch:9200"]'
      ELASTICSEARCH_SERVICEACCOUNTTOKEN: ${KIBANA_SERVICE_TOKEN}
      XPACK_SECURITY_ENABLED: "true"
      XPACK_ENCRYPTEDSAVEDOBJECTS_ENCRYPTIONKEY: ${KIBANA_ENCRYPTED_SAVED_OBJECTS_KEY:-reservas-otel-dev-encrypted-saved-objects-key-32}
      XPACK_SECURITY_ENCRYPTIONKEY: ${KIBANA_SECURITY_ENCRYPTION_KEY:-reservas-otel-dev-security-encryption-key-32}
      XPACK_REPORTING_ENCRYPTIONKEY: ${KIBANA_REPORTING_ENCRYPTION_KEY:-reservas-otel-dev-reporting-encryption-key-32}
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch

  apm-server:
    image: docker.elastic.co/apm/apm-server:9.3.3
    command: ["--strict.perms=false", "-e"]
    environment:
      ELASTICSEARCH_USERNAME: ${ELASTICSEARCH_USERNAME:-elastic}
      ELASTICSEARCH_PASSWORD: ${ELASTIC_PASSWORD:-changeme}
    volumes:
      - ./observability/apm-server/apm-server.yml:/usr/share/apm-server/apm-server.yml:ro
    ports:
      - "8200:8200"
    depends_on:
      - elasticsearch

  elastic-agent:
    image: docker.elastic.co/elastic-agent/elastic-agent:9.3.3
    command: ["elastic-agent", "run", "-e", "-c", "/usr/share/elastic-agent/elastic-agent.yml"]
    user: root
    environment:
      FLEET_ENROLL: "0"
      ELASTICSEARCH_USERNAME: ${ELASTICSEARCH_USERNAME:-elastic}
      ELASTICSEARCH_PASSWORD: ${ELASTIC_PASSWORD:-changeme}
    volumes:
      - ./observability/elastic-agent/elastic-agent.yml:/usr/share/elastic-agent/elastic-agent.yml:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - elasticsearch

  room-service:
    environment:
      ELASTIC_APM_SERVER_URL: http://apm-server:8200
      ELASTIC_APM_SERVICE_NAME: room-service
      ELASTIC_APM_ENVIRONMENT: dev
      ELASTIC_APM_SERVICE_VERSION: 0.1.0
      ELASTIC_APM_ENABLE_LOG_CORRELATION: "true"

  user-service:
    environment:
      ELASTIC_APM_SERVER_URL: http://apm-server:8200
      ELASTIC_APM_SERVICE_NAME: user-service
      ELASTIC_APM_ENVIRONMENT: dev
      ELASTIC_APM_SERVICE_VERSION: 0.1.0
      ELASTIC_APM_ENABLE_LOG_CORRELATION: "true"

  reservation-service:
    environment:
      ELASTIC_APM_SERVER_URL: http://apm-server:8200
      ELASTIC_APM_SERVICE_NAME: reservation-service
      ELASTIC_APM_ENVIRONMENT: dev
      ELASTIC_APM_SERVICE_VERSION: 0.1.0
      ELASTIC_APM_ENABLE_LOG_CORRELATION: "true"

  notification-service:
    environment:
      ELASTIC_APM_SERVER_URL: http://apm-server:8200
      ELASTIC_APM_SERVICE_NAME: notification-service
      ELASTIC_APM_ENVIRONMENT: dev
      ELASTIC_APM_SERVICE_VERSION: 0.1.0
      ELASTIC_APM_ENABLE_LOG_CORRELATION: "true"

  api-gateway:
    environment:
      ELASTIC_APM_SERVER_URL: http://apm-server:8200
      ELASTIC_APM_SERVICE_NAME: api-gateway
      ELASTIC_APM_ENVIRONMENT: dev
      ELASTIC_APM_SERVICE_VERSION: 0.1.0
      ELASTIC_APM_ENABLE_LOG_CORRELATION: "true"
